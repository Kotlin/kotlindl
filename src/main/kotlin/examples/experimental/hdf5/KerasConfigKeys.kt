package examples.experimental.hdf5

class KerasConfigKeys {
    private val version = "keras_version"
    private val backend = "backend"
    private val className = "class_name"
    private val classNameModelType = "Sequential"
    private val classNameModel = "Model"
    private val config = "config"
    private val layers = "layers"
    private val inputLayers = "input_layers"
    private val outputLayers = "output_layers"
    private val loss = "loss"
    private val modelWeights = "model_weights"
    private val modelConfig = "model_config"
    private val trainingConfig = "training_config"
    private val optimizerConfig = "optimizer_config"
}